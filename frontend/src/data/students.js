// These constants are used by the frontend for geofencing logic.
// Student list is now fetched from the backend API.

export const GEOFENCE_RADIUS_METERS = 100; // Used for live background monitoring
export const INITIAL_SCAN_RADIUS_METERS = 200; // Used for Teacher-to-Student initial scan
export const MONITORING_DURATION_MS = 120000;
